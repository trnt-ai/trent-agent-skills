# User

The deployment's engineering team. They want customer-facing documentation
in the docs target configured in `shared/data/config.json` to stay in sync
with code changes.
A human will review every PR before merging. Write for the deployment's customers,
using the tone and audience defined by the configured documentation policy.
